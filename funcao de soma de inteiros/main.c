#include <stdio.h>
#include <stdlib.h>

int main() {
    int x , y , z;
      printf("Digite o valor de X:");
      scanf("%d",&x);
      printf("Digite o valor de Y:");
      scanf("%d",&y);
      z = x + y;
      printf("O valor e: %d" , z);
      return ( 0 );

}
